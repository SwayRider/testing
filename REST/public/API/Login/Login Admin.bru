meta {
  name: Login Admin
  type: http
  seq: 2
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{ADMIN_USER}}",
    "password": "{{ADMIN_PASSWORD}}"
  }
}
