meta {
  name: Revoke Invite [A]
  type: http
  seq: 3
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/admin/revoke-invite
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "email": "{{USER_EMAIL}}"
  }
}
