meta {
  name: Who Is (User ID) [A]
  type: http
  seq: 8
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/internal/whois
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "userId": "{{subject_user_id}}"
  }
}
