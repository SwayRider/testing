meta {
  name: Who Is (Email) [A]
  type: http
  seq: 7
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/internal/whois
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "email": "{{USER_EMAIL}}"
  }
}

script:post-response {
  if (res.status == 200) {
    if (res.body.userId !== undefined) {
      bru.setVar("subject_user_id", res.body.userId);
    }
  }
}
