name: Check Password Strength
description: Performs an entrpy check on the provided password
method: POST
url: ${API_SCHEMA}://${API_HOST}:${API_PORT}/api/v1/auth/check-password-strength
body:
  content: |-
    {
      "password":"CheckMe"
    }
  content_type: application/json
headers:
- name: content-type
  value: application/json
