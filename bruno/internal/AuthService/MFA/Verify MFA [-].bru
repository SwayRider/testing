meta {
  name: Verify MFA [-]
  type: http
  seq: 5
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/mfa/verify
  body: json
  auth: inherit
}

body:json {
  {
    "mfaToken": "{{mfa_token}}",
    "code": "{{mfa_code}}",
    "rememberMe": false
  }
}
