meta {
  name: Request Password Reset [-]
  type: http
  seq: 1
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/request-password-reset
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{USER_EMAIL}}"
  }
}
