meta {
  name: Logout [L]
  type: http
  seq: 4
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/logout
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
