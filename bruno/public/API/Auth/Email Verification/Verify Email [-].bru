meta {
  name: Verify Email [-]
  type: http
  seq: 1
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/verify-email
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{USER_EMAIL}}",
    "verification_url": "{{API_HOST}}:{{API_PORT}}/web"
  }
}
