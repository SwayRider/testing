meta {
  name: Refresh Token [L]
  type: http
  seq: 3
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
