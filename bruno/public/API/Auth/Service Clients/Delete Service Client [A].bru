meta {
  name: Delete Service Client [A]
  type: http
  seq: 3
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/admin/delete-service-client
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "clientId": "{{service_client_id}}"
  }
}
