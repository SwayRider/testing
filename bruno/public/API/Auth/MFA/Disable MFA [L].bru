meta {
  name: Disable MFA [L]
  type: http
  seq: 7
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/mfa/disable
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "password": "{{USER_PASSWORD}}"
  }
}
