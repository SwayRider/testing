meta {
  name: Enable MFA [L]
  type: http
  seq: 2
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/mfa/enable
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "code": "{{mfa_code}}"
  }
}

script:post-response {
  if (res.status == 200 && res.body.backup_codes !== undefined) {
    bru.setVar("mfa_backup_codes", res.body.backup_codes.join(", "));
  }
}
