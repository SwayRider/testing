meta {
  name: Verify MFA [-]
  type: http
  seq: 5
}

post {
  url: {{API_HOST}}:{{API_PORT}}/api/v1/auth/mfa/verify
  body: json
  auth: inherit
}

body:json {
  {
    "mfa_token": "{{mfa_token}}",
    "code": "{{mfa_code}}",
    "remember_me": false
  }
}
