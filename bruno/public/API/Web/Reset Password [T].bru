meta {
  name: Reset Password [T]
  type: http
  seq: 5
}

get {
  url: {{API_HOST}}:{{API_PORT}}/web/reset-password?u={{reset_password_user_id}}&t={{reset_password_token}}
  body: none
  auth: inherit
}
